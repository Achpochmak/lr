
  <div class="box">
  	<article class="media">
<div class="media-content">
  			<div class="content">
  				<strong> <%= t('new.post')%></strong>
  				<p><%= @post.post %></p>
  			</div>
        </div>

<%= link_to t('new.edit'), edit_post_path(@post), class: "button is-primary" %> |
<%= link_to t('new.back'), posts_path, class: "button is-primary" %>
<%= button_to t("new.destroy"), @post, method: :delete , class: "button is-primary"%>
</div>
</article>